# خطة تطوير اللوحة الإدارية — QuickData

> **ملف توثيق حي** — أي محادثة جديدة (أو مساعد ذكاء اصطناعي جديد) يجب أن تقرأ هذا الملف أولاً قبل أي تعديل.
> المشروع الحقيقي والمعتمد هو نفس الملفات المرفقة هنا: theme.css, layout.js, supabase.js, login.html,
> index.html, tasks.html, ideas.html, contracts.html, vehicles.html, وملفات admin_schema_stepN.sql.

## 0. تحديث 2026-09-08 (أ) — تصحيح مهم بخصوص "حراء للسياحة" (مُنفَّذ بالكامل)

كان فيه خطأ متكرر في نسخة سابقة من الملفات: عبارات وتعليقات بتقول إن تسجيل الدخول ومشروع Supabase
بتوع QuickData **مشتركين** مع نظام "حراء للسياحة" المحاسبي. **العميل صحّح الأمر صراحةً: ده غير صحيح.**
QuickData مشروع Supabase مستقل تمامًا (QuickData ProSoft V.1, project_id: hecsuitrqtbzgbbjjtbf)،
وله مستخدموه الخاصون، ومفيش أي مشاركة بيانات دخول مع أي نظام آخر.

تم تنظيف كل الإشارات الخاطئة دي من 6 ملفات (login.html — بما فيها **نص كان ظاهر فعليًا للمستخدم**
في صفحة الدخول —, layout.js, theme.css, supabase.js, admin_schema_step1.sql, STEP1_README.md).
تم التأكد بـ grep شامل على كل ملفات الكود: **لا توجد أي إشارة متبقية**.

**أي محادثة جديدة: لو شفت أي إشارة لـ"حراء" في أي ملف تاني من المشروع، امسحها فورًا.**

## 0. تحديث 2026-09-08 (ب) — موديول السيارات مكتمل الآن (واجهة + قاعدة)

- أُضيف عمود `insurance_expiry` لجدول `admin_vehicles` (كان ناقصًا رغم طلب العميل الصريح له في
  التسجيل الصوتي) — موثّق في `admin_schema_step5.sql` ومُنفَّذ فعليًا على القاعدة.
- **`vehicles.html`** صفحة جديدة كاملة على نفس نمط `contracts.html`:
  - فورم إضافة سيارة (رقم اللوحة، النوع، الفرع، الرقم التسلسلي، 3 تواريخ انتهاء).
  - 5 تبويبات: الكل / نشطة / مركونة / معروضة للبيع / منتهية-خارج الخدمة — تغطي كل الحالات
    المطلوبة في التسجيل الصوتي ("مركونة"، "بيتم بيعها"، "انتهت").
  - **فلتر بالفرع** فوق الجدول — يحقق طلب العميل "فصل كل فرع لوحده" من غير الحاجة لصفحة منفصلة لكل فرع.
  - تنبيهات تلقائية (client-side، بنفس منطق tasks/contracts) لكل واحد من الثلاثة مستقلًا: الاستمارة،
    الفحص، **التأمين** — تُسجَّل في `admin_notifications` بفارق 30 يوم قبل الانتهاء + عند الانتهاء الفعلي.
  - حالة كل سيارة قابلة للتغيير مباشرة من الجدول (select) زي عمود الحالة في tasks.html.
- تم ربط رابط "السيارات" فعليًا (بدل "قريبًا") في السايدبار وكارت الاختصار في **كل** الصفحات:
  index.html, tasks.html, ideas.html, contracts.html, vehicles.html نفسها.
- `index.html`: كارت الإحصائيات بيعرض دلوقتي **عدد السيارات النشطة فعليًا** بدل "— (قريبًا)".

## 0. تحديث 2026-09-08 (ج) — موديول المشتريات مكتمل الآن (واجهة كاملة)

- **`purchases.html`** صفحة جديدة كاملة على نفس نمط `contracts.html` / `vehicles.html`، لكنها تجمع
  3 جداول مرتبطة في صفحة واحدة (المشتريات + الموردين + عروض الأسعار) عبر **تبويبات فرعية** داخل
  الصفحة نفسها (بدل 3 صفحات منفصلة)، لأن الموديول واحد منطقيًا حسب التسجيل الصوتي والخطة الأصلية.
  - **تبويب المشتريات**: فورم إضافة (تاريخ، نوع الشراء، مورد [اختياري من admin_suppliers]، فرع
    [اختياري]، وصف الصنف، التكلفة، رابط مرفق/فاتورة اختياري، ملاحظات) + **فلتر بالفرع** (نفس نمط
    vehicles.html) + جدول بكل العمليات + **زرار طباعة تقرير** (يفتح `window.print()` بتنسيق مخصص
    للطباعة يخفي السايدبار/الفورم/التبويبات ويظهر رأس تقرير رسمي) — تنفيذًا للمطلوب صراحة في
    التسجيل الصوتي (طباعة تقرير مفصّل للإدارة، بند 4 في "الأولويات المفتوحة" سابقًا.
  - **تبويب الموردين**: فورم إضافة (اسم، نشاط، موبايل، تاريخ تسجيل) + جدول يعرض `supplier_code`
    التسلسلي التلقائي كمرجع سريع لكل مورد + حذف.
  - **تبويب عروض الأسعار**: فورم إضافة (اسم العرض، تاريخ، اسم الجهة، ملاحظات) + حالة قابلة للتغيير
    مباشرة من الجدول (select) بنفس نمط عمود الحالة في tasks.html.
    **⚠️ قرار غير مؤكَّد من العميل**: قيم الحالة المستخدمة هي `under_review` (قيد الدراسة) /
    `accepted` (مقبول) / `rejected` (مرفوض) — التسجيل الصوتي لم يحدد قيم الحالة بالظبط لعروض
    الأسعار، فهذا افتراض معقول مبني على نمط باقي الموديولات. يحتاج تأكيد من العميل.
  - كروت إحصائية أعلى الصفحة: إجمالي المشتريات، إجمالي التكلفة، عدد الموردين، عدد عروض الأسعار.
  - **لا يوجد نظام تنبيهات تلقائي لهذا الموديول** (لا مواعيد انتهاء مرتبطة بالمشتريات/الموردين/عروض
    الأسعار في التوصيف الأصلي، بعكس العقود والسيارات).
- تم ربط رابط "المشتريات" فعليًا (بدل "قريبًا") في السايدبار وكارت الاختصار في **كل** الصفحات:
  index.html, tasks.html, ideas.html, contracts.html, vehicles.html, purchases.html نفسها.
- `index.html`: كارت إحصائيات جديد "إجمالي المشتريات" بدل الاعتماد على placeholder.
- **`admin_schema_step6.sql`** (جديد، توثيقي بالكامل): الجداول الثلاثة (admin_suppliers,
  admin_purchases, admin_price_offers) كانت موجودة بالفعل على القاعدة الفعلية قبل بناء الواجهة
  (كما هو موثّق في القسم 3 أدناه) — الملف ده لإعادة الإنشاء في بيئة أخرى فقط، وليس مطلوبًا تشغيله
  على القاعدة الحالية.

## 0. تحديث 2026-09-08 (د) — تأكيد قيم حالة عروض الأسعار + بناء موديول استلام/تسليم الأعمال

- **قيم حالة عروض الأسعار مُثبَّتة الآن** (`under_review` قيد الدراسة / `accepted` مقبول /
  `rejected` مرفوض) — تم تأكيدها كقرار نهائي بدل ما تفضل معلّقة. القيم دي بالفعل مطبّقة في كود
  `purchases.html` من قبل ولا تغيير مطلوب على الواجهة أو القاعدة؛ التغيير هنا توثيقي فقط (نقلها من
  "افتراض غير مؤكَّد" إلى "قرار معتمد"). لو العميل عايز أسامي تانية لاحقًا، التغيير بسيط (تعديل
  القيم في select + الجدول).
- **`handovers.html`** صفحة جديدة كاملة على نفس نمط باقي الموديولات، لموديول استلام/تسليم الأعمال:
  - فورم إضافة سجل (اسم الموظف، الاتجاه [استلام/تسليم]، الفرع، التاريخ) + شيك ليست للعهدة
    (موبايل، سيارة، بريد إلكتروني، بيانات هندسية) + حقل نصي حر لأي أصناف أخرى + ملاحظات + خانة
    "موقّع بالفعل" عند الإضافة.
  - **فلتر بالفرع** فوق الجدول (نفس نمط vehicles.html/purchases.html).
  - تبويبات: الكل / استلام / تسليم.
  - عمود التوقيع قابل للتغيير مباشرة من الجدول (select موقّع/غير موقّع) بنفس نمط عمود الحالة في
    باقي الموديولات.
  - كروت إحصائية: إجمالي السجلات، عدد الاستلام، عدد التسليم، عدد السجلات بدون توقيع.
  - **لا يوجد نظام تنبيهات تلقائي لهذا الموديول** (لا تواريخ انتهاء مرتبطة بالاستلام/التسليم).
- تم ربط رابط "استلام وتسليم الأعمال" في السايدبار في **كل** الصفحات: index.html, tasks.html,
  ideas.html, contracts.html, vehicles.html, purchases.html, handovers.html نفسها.
- `index.html`: كارت إحصائيات جديد "سجلات استلام/تسليم" + كارت اختصار للموديول.
- **`admin_schema_step7.sql`** (جديد، توثيقي بالكامل): جدول admin_handovers كان موجود بالفعل على
  القاعدة الفعلية قبل بناء الواجهة (كما هو موثّق في القسم 3 و4 أدناه) — الملف ده لإعادة الإنشاء في
  بيئة أخرى فقط، وليس مطلوبًا تشغيله على القاعدة الحالية.
- **الموديول الوحيد المتبقي من غير واجهة الآن: إدارات أخرى (تواصل بين الأقسام) — admin_correspondence.**

## 0. تحديث 2026-09-08 (هـ) — بناء موديول التواصل بين الإدارات (آخر موديول متبقي)

- **`correspondence.html`** صفحة جديدة كاملة، آخر موديول من السبعة المطلوبة أصلًا:
  - فورم إضافة مخاطبة (الموضوع، القسم [نص حر مع اقتراحات تلقائية من الأقسام المستخدمة سابقًا]،
    التاريخ، ملاحظات).
  - **فلتر بالقسم** فوق الجدول (مبني ديناميكيًا من القيم الفعلية المسجّلة، لأنه مفيش جدول أقسام
    منفصل في التوصيف الأصلي — القسم نص حر في admin_correspondence نفسه).
  - تبويبات: الكل / قيد الانتظار / قيد المتابعة / تم الإنجاز.
  - عمود الحالة قابل للتغيير مباشرة من الجدول، بنفس نمط باقي الموديولات.
  - كروت إحصائية: إجمالي المخاطبات، قيد الانتظار، قيد المتابعة، تم الإنجاز.
  - **لا يوجد نظام تنبيهات تلقائي لهذا الموديول** (لا تواريخ انتهاء مرتبطة بالمخاطبات).
  - ⚠️ **قيم حالة المخاطبة** (`pending`/`in_progress`/`resolved`) افتراض معقول بنفس نمط باقي
    الموديولات — التسجيل الصوتي لم يحدد قيم حالة بالظبط لهذا الموديول تحديدًا (كان بيوصف الفكرة
    العامة فقط). القيم دي قابلة للتعديل بسهولة لو العميل عايز مسميات مختلفة، لكنها مش بند حرج.
- تم ربط رابط "التواصل بين الإدارات" في السايدبار في **كل** الصفحات: index.html, tasks.html,
  ideas.html, contracts.html, vehicles.html, purchases.html, handovers.html, correspondence.html
  نفسها.
- `index.html`: كارت إحصائيات جديد "مخاطبات قيد الانتظار" + كارت اختصار للموديول.
- **`admin_schema_step8.sql`** (جديد، توثيقي بالكامل): جدول admin_correspondence كان موجود بالفعل
  على القاعدة الفعلية قبل بناء الواجهة (كما هو موثّق في القسم 3 و4 أدناه) — الملف ده لإعادة الإنشاء
  في بيئة أخرى فقط، وليس مطلوبًا تشغيله على القاعدة الحالية.
- **🎉 كل الموديولات السبعة الأصلية بقى ليها واجهة شغّالة الآن.** الأولويات المفتوحة المتبقية كلها
  بانتظار توجيه العميل (راجع قسم 8) وليست بناء واجهات جديدة.

## 1. معلومات المشروع (Supabase)
- اسم المشروع: **QuickData ProSoft V.1**
- project_id: hecsuitrqtbzgbbjjtbf
- Project URL: https://hecsuitrqtbzgbbjjtbf.supabase.co
- **مشروع مستقل تمامًا** — لا يشارك أي باك إند أو بيانات دخول مع أي نظام آخر.
- يُدار عبر **Supabase MCP Connector** — التنفيذ يتم مباشرة على القاعدة الفعلية (لازم توصيل
  الـ connector أولًا في أي محادثة جديدة قبل تنفيذ أي SQL).
- **كل الجداول ببادئة admin_ وعمود id من نوع uuid** — هذا هو التوافق المطلوب لأي جدول جديد.

## 2. مصدر المتطلبات
تفريغ صوتي من الأستاذ محمد يصف 7 موديولات: المهام، السيارات، المشتريات (+موردين+عروض أسعار)،
تطوير الأفكار، استلام/تسليم الأعمال، إدارات أخرى (تواصل بين الأقسام)، العقود.

**⚠️ التسجيل الصوتي غير مكتمل** — ينقطع فجأة عند وصف "sheet رقم اتنين" في موديول تطوير الأفكار.
**لسه مفيش إجابة من العميل** هل فيه شيت تاني مطلوب في الموديول ده ولا لأ.

## 3. حالة الموديولات (مؤكَّدة بالفحص المباشر عبر Supabase MCP، آخر تحديث 2026-09-08)

| الموديول | الجدول | الحالة | واجهة (.html)؟ |
|---|---|---|---|
| الأساس (فروع + إشعارات مركزية) | admin_branches, admin_notifications | ✅ | index.html |
| المهام (تنسيق الأعمال) | admin_tasks | ✅ | ✅ tasks.html |
| **السيارات** | admin_vehicles (+insurance_expiry) | ✅ | ✅ **vehicles.html (جديد)** |
| **المشتريات** | admin_purchases, admin_suppliers, admin_price_offers | ✅ | ✅ **purchases.html (جديد)** |
| **استلام/تسليم الأعمال** | admin_handovers | ✅ | ✅ **handovers.html (جديد)** |
| **إدارات أخرى (تواصل)** | admin_correspondence | ✅ | ✅ **correspondence.html (جديد)** |
| تطوير الأعمال والأفكار | admin_ideas | ✅ | ✅ ideas.html (⚠️ راجع قسم 2) |
| العقود | admin_contracts | ✅ | ✅ contracts.html |

**كل الموديولات السبعة ليها واجهة شغّالة الآن: تنسيق الأعمال، السيارات، المشتريات،
استلام/تسليم الأعمال، التواصل بين الإدارات، تطوير الأفكار، العقود.**

## 4. تفاصيل بنية الجداول (كما هي فعليًا في القاعدة)
- admin_branches(id uuid, name, is_active, created_at)
- admin_notifications(id uuid, module, title, message, level, ref_table, ref_id, is_read, created_at)
- admin_tasks(id uuid, title, coordinated_with, priority[critical/important/deferrable],
  status[pending/deferred/done/transferred], notes, created_at, updated_at)
- admin_vehicles(id uuid, plate_number, car_type, branch_id→admin_branches, sequence_number,
  license_expiry, inspection_expiry, insurance_expiry [مضاف 2026-09-08], status[active/parked/
  for_sale/retired — الواجهة تعرضها بأسماء عربية، القاعدة تخزن 'active' افتراضيًا], notes,
  created_at, updated_at)
- admin_suppliers(id uuid, supplier_code bigint [تسلسلي تلقائي], name, activity, mobile,
  registration_date, created_at)
- admin_purchases(id uuid, purchase_date, purchase_type, supplier_id→admin_suppliers,
  branch_id→admin_branches, item_description, cost numeric, attachment_url, notes, created_at)
- admin_price_offers(id uuid, offer_name, offer_date, entity_name,
  status[under_review/accepted/rejected — مؤكَّد 2026-09-08 (د)], notes, created_at)
- admin_handovers(id uuid, employee_name, direction, has_mobile, has_car, has_email,
  has_engineering, other_items, branch_id→admin_branches, handover_date, signed, notes, created_at)
- admin_correspondence(id uuid, subject, department [نص حر], correspondence_date,
  status[pending/in_progress/resolved — افتراض مطبَّق 2026-09-08 (هـ)، غير مؤكَّد من العميل بالظبط],
  notes, created_at)
- admin_ideas(id uuid, title, department, status[new/under_review/approved/rejected/implemented],
  notes, created_at, updated_at)
- admin_contracts(id uuid, contract_type, owner_name, branch_id→admin_branches, contract_date,
  expiry_date [إضافة زيادة عن التوصيف الأصلي], notes, created_at, updated_at)

## 5. منطق التنبيهات المتدرجة (تنفيذ فعلي مؤكَّد من الكود، client-side في كل الحالات)
- **المهام** (tasks.html): NOTIFY_UNFINISHED_AFTER_DAYS=2, NOTIFY_OVERDUE_AFTER_DAYS=7.
- **العقود** (contracts.html): تنبيه قبل 30 يوم من expiry_date.
- **السيارات** (vehicles.html): تنبيه قبل 30 يوم لكل من license_expiry/inspection_expiry/
  insurance_expiry مستقلًا عن بعض، + تنبيه "منتهي" عند تجاوز التاريخ.
- كل ده client-side (يُفحص عند فتح الصفحة)، **مش cron على السيرفر** — pg_cron extension متاحة
  في المشروع بس غير مُفعّلة.
- **المشتريات** (purchases.html): لا يوجد منطق تنبيهات لهذا الموديول (لا تواريخ انتهاء مرتبطة).
- **استلام/تسليم الأعمال** (handovers.html): لا يوجد منطق تنبيهات لهذا الموديول (لا تواريخ انتهاء
  مرتبطة).
- **التواصل بين الإدارات** (correspondence.html): لا يوجد منطق تنبيهات لهذا الموديول (لا تواريخ
  انتهاء مرتبطة).

## 6. ملفات المشروع (2026-09-08)
theme.css, layout.js, supabase.js, login.html, index.html, tasks.html, ideas.html,
contracts.html, vehicles.html, purchases.html, handovers.html, **correspondence.html (جديد)**,
admin_schema_step1.sql (فروع+إشعارات),
admin_schema_step2.sql (مهام), admin_schema_step3.sql (أفكار، توثيقي),
admin_schema_step4.sql (عقود، توثيقي), admin_schema_step5.sql (تصحيح عمود تأمين السيارات، توثيقي),
admin_schema_step6.sql (مشتريات+موردين+عروض أسعار، توثيقي),
admin_schema_step7.sql (استلام/تسليم الأعمال، توثيقي),
**admin_schema_step8.sql (جديد، التواصل بين الإدارات، توثيقي)**,
STEP1_README.md, STEP2_README.md.

**كل صفحة جديدة يجب أن تتبع نفس النمط**: تربط theme.css بسطر واحد، تستدعي initAdminPage() من
layout.js، وتستخدم دوال supabase.js العامة بدل عمل Supabase client مستقل.

## 7. سجل التصحيحات والإضافات — 2026-09-08
1. إصلاح إشارات "حراء للسياحة" في 6 ملفات (قسم 0-أ).
2. إضافة عمود insurance_expiry لجدول admin_vehicles (قسم 0-ب).
3. بناء موديول السيارات كاملًا (واجهة vehicles.html + ربط الروابط في كل الصفحات + كارت الإحصائيات
   في index.html) (قسم 0-ب).
4. بناء موديول المشتريات كاملًا (واجهة purchases.html بثلاث تبويبات فرعية + ربط الروابط في كل
   الصفحات + كارت إحصائيات في index.html + زرار طباعة تقرير) (قسم 0-ج).
5. تثبيت قيم حالة عروض الأسعار كقرار نهائي (بدل معلّق) (قسم 0-د).
6. بناء موديول استلام/تسليم الأعمال كاملًا (واجهة handovers.html + ربط الروابط في كل الصفحات +
   كارت إحصائيات في index.html) (قسم 0-د).
7. بناء موديول التواصل بين الإدارات كاملًا (واجهة correspondence.html + ربط الروابط في كل الصفحات
   + كارت إحصائيات في index.html) — آخر موديول من السبعة الأصلية (قسم 0-هـ).

## 8. الأولويات المفتوحة (بانتظار توجيه العميل/المستخدم)
1. الرجوع للعميل لتأكيد وجود/عدم وجود "شيت تاني" في موديول تطوير الأفكار (التسجيل الصوتي غير مكتمل).
2. تفعيل pg_cron لو العميل عايز فحص تنبيهات دوري تلقائي من السيرفر بدل الاعتماد على فتح الصفحة.
3. **تأكيد قيم حالة المخاطبات** (قيد الانتظار/قيد المتابعة/تم الإنجاز) مع العميل — افتراض غير حرج
   مبني على نمط باقي الموديولات، سهل التعديل لو احتاج مسميات مختلفة (راجع قسم 0-هـ).

**لا توجد موديولات بدون واجهة الآن — كل الأولويات المتبقية قرارات صغيرة بانتظار توجيه العميل، مش
بناء واجهات جديدة.**

## 9. تنبيه تاريخي (لتفادي تكرار الخطأ)
في مرحلة سابقة، تم بناء مجموعة جداول موازية بأسماء مختلفة (tasks, task_notifications, cars,
car_notifications, suppliers, purchases, price_quotes, business_ideas, handover_forms,
interdepartmental_items, contracts) — **تم حذفها بالكامل** لأنها كانت مكررة وفارغة. لا داعي
لإعادة إنشائها. الجداول الصحيحة الوحيدة هي كل ما ببادئة admin_.
