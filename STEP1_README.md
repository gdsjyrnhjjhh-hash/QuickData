# الخطوة 1 من 8 — الأساس المشترك للوحة الإدارية الجديدة

## خلفية سريعة: ليه مشروع منفصل؟

النظام المرفوع أصلاً (`hraa-tourism-system`) هو **نظام محاسبي وحجوزات سياحية** (خزينة، رحلات، عملاء، موردين، سندات...).
اللي طلبه العميل في التسجيل الصوتي حاجة تانية تمامًا: **لوحة تحكم إدارية/تشغيلية داخلية** (تنسيق أعمال، سيارات، مشتريات، تطوير أفكار، عقود).
لذلك بنبنيها كمشروع منفصل بالكامل (مجلد/repo جديد: `hraa-admin-dashboard`)، لكن:
- **بنفس الباك إند** (نفس مشروع Supabase، نفس المستخدمين، نفس تسجيل الدخول) — مفيش حاجة تتضاف من ناحية المستخدمين.
- **بنفس الهوية البصرية 100%** المستخدمة فعليًا في `index.html` الحالي (مش ملفات `theme.css`/`layout.js` القديمة اللي كانت موجودة بالمشروع الأصلي بدون ما تُستخدم فعليًا في أي صفحة — تم استخراج التصميم الحقيقي المستخدم فعلاً وتحويله لملفات مشتركة قابلة لإعادة الاستخدام).

## إيه اللي اتعمل في الخطوة دي؟

1. **مجلد مشروع جديد** `hraa-admin-dashboard/` منفصل تمامًا عن نظام المحاسبة.
2. **`theme.css`**: التصميم الموحّد (سايدبار، هيدر، كروت إحصائية، جداول، مودالز، توست، وضع ليلي) — مستخرج من نفس تصميم `index.html` الفعلي، بحيث أي صفحة جديدة تربطه بسطر واحد `<link rel="stylesheet" href="theme.css">` بدل ما تكرر مئات الأسطر من CSS في كل صفحة.
3. **`layout.js`**: السلوكيات المشتركة (فتح/قفل السايدبار، الوضع الليلي، التنبيهات Toast، تسجيل الخروج، اختصارات لوحة المفاتيح) + دالة واحدة جاهزة `initAdminPage()` بتتنادى في أول كل صفحة جديدة فتظبط كل حاجة تلقائيًا.
4. **`supabase.js`**: نفس الملف من النظام الأصلي بالظبط (بدون أي تعديل) — لأنه يحتوي بالفعل على طبقة اتصال عامة بأي جدول (`fetchData` / `addData` / `updateData` / `deleteData`) ونظام الجلسة/تسجيل الدخول، وده كل اللي الموديولات الجديدة محتاجاه.
5. **`login.html`**: صفحة دخول جديدة بنفس منطق الدخول (نفس المستخدمين)، بس بهوية "اللوحة الإدارية".
6. **`index.html`**: الصفحة الرئيسية للوحة، وفيها:
   - السايدبار بكل الموديولات الخمسة القادمة (معلّم عليها "قريبًا" لحد ما نبنيها كل واحدة في خطوتها).
   - كروت إحصائية عامة.
   - **إدارة الفروع** (ميزة فعلية شغالة بالكامل من الآن): إضافة / تعديل الاسم / تفعيل-تعطيل / حذف فرع. الفروع دي هتُستخدم في كل الموديولات القادمة (بدل ما يتكتب اسم الفرع يدوي في كل مرة زي ما كان مذكور في التسجيل الصوتي).
   - زرار الإشعارات في الهيدر متصل فعليًا بجدول الإشعارات (لسه فاضي، الموديولات القادمة هي اللي هتملاه).
7. **`admin_schema_step1.sql`**: جدولين في نفس قاعدة بيانات Supabase الحالية:
   - `admin_branches`: الفروع.
   - `admin_notifications`: جدول إشعارات مركزي واحد هتستخدمه كل الموديولات القادمة (المهام، السيارات، العقود...) بدل ما كل موديول يعمل نظام إشعارات منفصل — وده أهم طلب اتكرر في التسجيل الصوتي.

## خطوات التشغيل (مهم جدًا قبل ما تجرب اللوحة)

1. افتح **Supabase Dashboard → SQL Editor** (نفس المشروع بتاع نظام حراء للسياحة المحاسبي).
2. افتح ملف **`admin_schema_step1.sql`** المرفق، انسخه بالكامل، الصقه في SQL Editor، واضغط **RUN**.
3. تأكد إن جدولين جداد ظهروا في Table Editor: `admin_branches`, `admin_notifications`.
4. ارفع مجلد `hraa-admin-dashboard` كامل على نفس الاستضافة (أو استضافة منفصلة) — هو مشروع HTML ثابت زي بالظبط النظام الأصلي، مفيش خطوة بناء (build) مطلوبة.
5. افتح `login.html` وسجّل دخولك بنفس بيانات دخولك المعتادة في نظام حراء للسياحة.

## للتجربة السريعة

1. بعد تسجيل الدخول هتوصل لـ `index.html` وتلاقي قسم **"إدارة الفروع"** في آخر الصفحة.
2. اكتب اسم فرع (مثلًا "الفرع الساحلي") واضغط **"إضافة الفرع"** → هيظهر فورًا في الجدول وفي كارت "الفروع المسجّلة" فوق.
3. جرّب تعديل الاسم (زرار القلم) وتعطيل/تفعيل الفرع (زرار المفتاح) وحذفه (زرار سلة المهملات).
4. جرّب الوضع الليلي وفتح/قفل السايدبار من الموبايل والديسكتوب — نفس سلوك النظام الأصلي بالظبط.
5. اضغط على أي موديول من الموديولات المعلّم عليها "قريبًا" (تنسيق الأعمال، السيارات...) هتلاقي رسالة توضح إنه هيتم بناؤه في خطوة قادمة — مفيش روابط مكسورة.

## ملحوظة مهمة

الخطوة دي هي الأساس اللي كل الخطوات الجايّة (المهام، السيارات، المشتريات، الأفكار، العقود) هتُبنى فوقه: نفس التصميم، نفس طريقة الاتصال بقاعدة البيانات، نفس جدول الفروع، ونفس جدول الإشعارات المركزي. بعد ما تتأكد إن كل حاجة في الخطوة دي شغالة تمام، نكمل على الخطوة 2.
