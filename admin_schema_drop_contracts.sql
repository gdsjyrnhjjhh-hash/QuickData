-- ================================================================
-- اللوحة الإدارية — QuickData
-- حذف موديول العقود (Contracts) بالكامل من القاعدة
-- انسخ هذا الملف بالكامل والصقه في: Supabase Dashboard → SQL Editor → New query
-- ثم اضغط RUN مرة واحدة.
--
-- ملاحظة: لا يوجد أي جدول آخر يعتمد على admin_contracts (لا FK من جهته
-- على أي جدول غير admin_branches، ولا أي جدول تاني بيرجع له)، فالحذف آمن
-- ومفيش تأثير على باقي الموديولات.
-- ================================================================

drop policy if exists "admin_contracts_all" on public.admin_contracts;
drop table if exists public.admin_contracts cascade;

-- ================================================================
-- تم! افتح Table Editor للتأكد إن جدول admin_contracts اتشال.
-- ================================================================
