# الخطوة 2 من 8 — تنسيق الأعمال (المهام)

## إيه اللي اتعمل في الخطوة دي؟

1. **جدول `admin_tasks`** جديد في قاعدة البيانات (ملف `admin_schema_step2.sql`).
2. **صفحة `tasks.html`** جديدة كاملة، وفيها:
   - **3 قوائم حسب الأولوية** بالظبط زي ما طلب العميل: مهام مهمة جدًا / مهمة فقط / ممكن تتأجل — كل قايمة في تبويب (Tab) منفصل مع عدّاد بجانبه.
   - أعمدة كل قايمة: **مسلسل، اسم المهمة، الجهة المنسّقة لها، الحالة، تاريخ الإضافة**.
   - **الحالة** قائمة اختيار مباشرة على كل صف: قيد التنفيذ / أُجّلت / نُفّذت / رُحّلت — تتغيّر فورًا من غير ما تفتح فورم منفصل.
   - **نظام التنبيه التلقائي** المطلوب بالظبط في التسجيل الصوتي:
     - أي مهمة **"قيد التنفيذ"** لمدة **يومين** من غير تنفيذ → توسم في الجدول بعلامة "**غير منجزة**" ويتسجّل لها إشعار في جرس الإشعارات.
     - لو فضلت **أسبوع** من غير تنفيذ → تتحول العلامة إلى "**متأخرة**" (أحمر) ويتسجّل إشعار أهم درجة.
     - كل مهمة بتتفحص تلقائيًا في كل مرة تفتح فيها الصفحة، والنظام بيتأكد إنه ميكررش نفس الإشعار للمهمة نفسها.
   - كروت إحصائية سريعة: عدد المهام قيد التنفيذ في كل قايمة + إجمالي المتأخر/غير المنجز.
3. تم **تفعيل رابط "تنسيق الأعمال"** في السايدبار وفي كارت الموديولات بالصفحة الرئيسية (مبقاش معلّم "قريبًا").
4. الصفحة الرئيسية (`index.html`) دلوقتي بتعرض **عدد المهام قيد التنفيذ فعليًا** بدل الـ placeholder اللي كان مكانه "—".
5. زرار الإشعارات 🔔 في الهيدر بقى شغّال فعليًا: بيعرض آخر إشعار مسجّل (سواء من مهمة متأخرة أو غير منجزة).

## خطوات التشغيل

1. افتح **Supabase Dashboard → SQL Editor**.
2. انسخ محتوى **`admin_schema_step2.sql`** كاملًا، الصقه، اضغط **RUN**.
3. تأكد إن جدول `admin_tasks` ظهر في Table Editor.
4. حدّث ملفات المشروع على الاستضافة (الملفات الجديدة/المعدّلة: `tasks.html` جديد، و`index.html` و`admin_schema_step2.sql`).

## للتجربة السريعة

1. من الصفحة الرئيسية اضغط على كارت "**تنسيق الأعمال**" أو من السايدبار.
2. أضف مهمة تجريبية باسم "مهمة مهمة جدًا" واختار الأولوية "مهمة جدًا" → هتلاقيها ظهرت فورًا في تبويب "مهام مهمة جدًا".
3. جرّب تغيير حالة المهمة من القائمة المنسدلة في عمود "الحالة" → التصنيف والإحصائيات بتتحدث فورًا.
4. **لتجربة التنبيه بسرعة من غير ما تستنى يومين فعليًا**: من Supabase → Table Editor → افتح جدول `admin_tasks` → عدّل عمود `created_at` لأي مهمة "قيد التنفيذ" وخليه تاريخ قبل 3 أيام (مثلاً) → ارجع للصفحة واعمل تحديث (زرار التحديث 🔄 أو Ctrl+R) → هتلاقي علامة "غير منجزة" ظهرت على المهمة، وإشعار جديد اتسجّل (جرب زرار 🔔).
5. عدّل `created_at` لتاريخ قبل 8 أيام لنفس الطريقة → هتلاقي العلامة اتحولت لـ"متأخرة" (أحمر).

## ملحوظة مهمة

حدود الأيام (يومين للـ"غير منجزة" وأسبوع للـ"متأخرة") متحكم فيها من متغيّرين في أول كود الصفحة (`NOTIFY_UNFINISHED_AFTER_DAYS` و`NOTIFY_OVERDUE_AFTER_DAYS`) — سهل تغييرهم لو العميل عايز مدة مختلفة.

الخطوة الجاية (3) هتكمل نفس موديول "تنسيق الأعمال" بإضافة: **نموذج استلام وتسليم الأعمال داخل الشركة** (موبايل، سيارة، إيميل، أعمال هندسية + توقيع الموظف) + **شيت التواصل بين الإدارات**.
