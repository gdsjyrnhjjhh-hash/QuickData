-- ================================================================
-- اللوحة الإدارية — QuickData
-- الخطوة 5: تصحيح — إضافة عمود "انتهاء التأمين" لجدول السيارات
-- انسخ هذا الملف بالكامل والصقه في: Supabase Dashboard → SQL Editor → New query
-- ثم اضغط RUN مرة واحدة. آمن لإعادة التشغيل (IF NOT EXISTS في كل مكان).
--
-- ملاحظة: تم تنفيذ هذا التعديل بالفعل مباشرة على القاعدة الفعلية عبر
-- Supabase MCP Connector بتاريخ 2026-09-08. موجود هنا فقط للتوثيق
-- ولإعادة الإنشاء في بيئة أخرى (تطوير محلي، نسخة احتياطية، إلخ).
--
-- السبب: التسجيل الصوتي الأصلي للعميل ذكر صراحةً 3 تنبيهات مطلوبة
-- لكل سيارة: "انتهاء التأمين، قرب الاستمارة، قرب الفحص" — لكن جدول
-- admin_vehicles كان فيه بس license_expiry و inspection_expiry،
-- وناقص عمود التأمين بالكامل.
-- ================================================================

alter table public.admin_vehicles
    add column if not exists insurance_expiry date;

create index if not exists idx_admin_vehicles_insurance_expiry
    on public.admin_vehicles (insurance_expiry);

-- ================================================================
-- تم! افتح Table Editor → admin_vehicles للتأكد إن عمود
-- insurance_expiry ظهر بنجاح.
-- عند بناء واجهة السيارات (vehicles.html) لاحقًا، لازم فورم الإضافة
-- والتعديل يشمل حقل تاريخ انتهاء التأمين زي باقي التاريخين بالظبط،
-- ومنطق التنبيهات في admin_notifications يفحص الثلاثة معًا.
-- ================================================================
